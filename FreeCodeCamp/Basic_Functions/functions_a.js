function favoriteAnimal(animal) { // function definition with parameter (animal)
    return animal + " is my favorite animal!"
  }
  
  console.log(favoriteAnimal('Goat')); // Goat is the argument and we are telling favoriteAnimal function to send the Goat to it

/* In JavaScript, parameters are the items listed between the parentheses () in the function declaration. Function arguments are the actual values you decide to pass to the function */
