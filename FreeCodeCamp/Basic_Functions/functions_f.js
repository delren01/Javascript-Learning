/* Arrow functions are defined using the => syntax.
const add = (a, b) => {
  return a + b;
} */

/* the arrow function add takes two parameters a and b and returns their sum. The return keyword is used to return the result of the addition operation */

/* If the arrow function has only one expression, the curly braces {} and the return keyword can be omitted. The expression will be implicitly returned. */
// const add => (a, b) => a + b;